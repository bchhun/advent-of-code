[tool.poetry]
name = "advent-of-code"
version = "0.1.0"
description = ""
authors = ["bchhun <bchhun@cortexmedia.ca>"]

[tool.poetry.dependencies]
python = "^3.7"

[tool.poetry.dev-dependencies]
pylint = "^2.4.4"

[build-system]
requires = ["poetry>=0.12"]
build-backend = "poetry.masonry.api"
